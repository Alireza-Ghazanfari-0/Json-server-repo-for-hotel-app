{
  "name": "json-server-api-hotelwebapp",
  "private": true,
  "version": "0.0.0",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch server/db.json --port $PORT --host 0.0.0.0"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "json-server": "^0.17.3"
  }
}
